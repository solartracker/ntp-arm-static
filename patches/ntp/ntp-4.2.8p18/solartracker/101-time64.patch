diff -r -u a/libntp/machines.c b/libntp/machines.c
--- a/libntp/machines.c	2024-05-07 07:21:37.000000000 -0400
+++ b/libntp/machines.c	2026-02-04 01:10:58.752074229 -0500
@@ -477,7 +477,7 @@
 #endif /* HAVE_SETTIMEOFDAY */
 #ifdef HAVE_STIME
 	if (rc && (SET_TOD_STIME == tod || !tod)) {
-		long tp = tvp->tv_sec;
+		time_t tp = tvp->tv_sec;
 
 		errno = 0;
 		rc = stime(&tp); /* lie as bad as SysVR4 */
diff -r -u a/ntpd/ntp_io.c b/ntpd/ntp_io.c
--- a/ntpd/ntp_io.c	2024-05-07 07:21:17.000000000 -0400
+++ b/ntpd/ntp_io.c	2026-02-04 01:34:09.250762682 -0500
@@ -3459,8 +3459,8 @@
 						pts.tv_nsec = (long)(ticks * 1e9 *
 								     sys_tick);
 					}
-					DPRINTF(4, ("fetch_timestamp: system nsec network time stamp: %ld.%09ld\n",
-						    pts.tv_sec, pts.tv_nsec));
+					DPRINTF(4, ("fetch_timestamp: system nsec network time stamp: %jd.%09ld\n",
+						    (intmax_t)pts.tv_sec, pts.tv_nsec));
 					nts = tspec_stamp_to_lfp(pts);
 				}
 				break;
